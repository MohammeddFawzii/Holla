import 'package:flutter/material.dart';

class GamesViewBody extends StatelessWidget {
  const GamesViewBody({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("واجهه الالعاب"),
    );
  }
}
