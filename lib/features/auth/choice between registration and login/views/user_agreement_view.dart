import 'package:flutter/material.dart';
import 'package:holla/features/auth/choice%20between%20registration%20and%20login/widgets/user_agreement_view_body.dart';

class UserAgreementView extends StatelessWidget {
  const UserAgreementView({super.key});
  static const id = 'UserAgreementUseView';

  @override
  Widget build(BuildContext context) {
    return const UserAgreementViewBody();
  }
}
